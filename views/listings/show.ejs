<% layout("layouts/boilerplate.ejs") -%>

<%
  const catIcons = {
    "mountains": "🏔",
    "trending": "🔥",
    "tropical": "🌴",
    "beachfront": "🏖",
    "islands": "🏝",
    "desert": "🏜",
    "arctic": "❄️",
    "farms": "🌾",
    "camping": "⛺️",
    "amazing pools": "🏊",
    "lakefront": "🏞",
    "luxury": "💎",
    "castles": "🏰",
    "entire homes": "🏠",
    "iconic cities": "🌆"
  };
%>

<section class="page page-show">
  <article class="show-card">
    <!-- Header -->
    <header class="show-header">
      <div class="show-header-bar"></div>
      <div class="show-header-text">
        <h1><%= listing.title %></h1>
        <p class="show-owner">Owned by <span><%= listing.owner.username %></span></p>
      </div>
    </header>

    <!-- Image -->
    <div class="show-image-wrapper">
      <img
        src="<%= listing.image.url %>"
        alt="<%= listing.title %>"
        class="show-image"
      />
    </div>

    <!-- Content -->
    <div class="show-body">
      <!-- Categories -->
      <!-- Display category tags if the listing has any categories assigned -->
      <% if (listing.categories && listing.categories.length > 0) { %>
      <div class="show-tags">
        <!-- Loop through each category and display it as a clickable tag -->
        <% listing.categories.forEach(function(categoryName) { %>
        <a
          href="/listings?category=<%= encodeURIComponent(categoryName) %>"
          class="tag-pill"
        >
          <!-- Display emoji icon for the category, or default tag emoji if not found -->
          <%= catIcons[categoryName] || "🏷" %> <%= categoryName %>
        </a>
        <% }) %>
      </div>
      <% } %>

      <!-- Description -->
      <p class="show-description"><%= listing.description %></p>

      <!-- Price + Location -->
      <div class="show-price-location">
        <div class="show-price">
          <span class="price-number">
            ₹<%= listing.price.toLocaleString('en-IN') %>
          </span>
          <span class="price-per">/ night</span>
        </div>
        <div class="show-location">
          <i class="bi bi-geo-alt-fill"></i>
          <span><%= listing.location %>, <%= listing.country %></span>
        </div>
      </div>

      <!-- Owner controls -->
      <% if (currUser && currUser._id.equals(listing.owner._id)) { %>
      <div class="show-owner-actions">
        <a
          href="/listings/<%= listing._id %>/edit"
          class="btn-primary btn-inline"
        >
          <i class="bi bi-pencil-square"></i>
          <span>Edit</span>
        </a>

        <form
          action="/listings/<%= listing._id %>?_method=DELETE"
          method="POST"
        >
          <button type="submit" class="btn-outline">
            <i class="bi bi-trash"></i>
            <span>Delete</span>
          </button>
        </form>
      </div>
      <% } %>

      <hr class="show-divider" />

      <!-- Review form -->
      <section class="show-reviews">
        <% if (currUser) { %>
        <form
          action="/listings/<%= listing._id %>/reviews"
          method="POST"
          novalidate
          class="form-vertical review-form"
        >
          <h2>Leave a review</h2>

          <div class="form-field">
            <label class="form-label" for="rating">Rating</label>
            <fieldset class="starability-slot">
              <input
                type="radio"
                id="no-rate"
                name="review[rating]"
                value="1"
                checked
              />

              <input
                type="radio"
                id="first-rate1"
                name="review[rating]"
                value="1"
              />
              <label for="first-rate1">1 star</label>

              <input
                type="radio"
                id="first-rate2"
                name="review[rating]"
                value="2"
              />
              <label for="first-rate2">2 stars</label>

              <input
                type="radio"
                id="first-rate3"
                name="review[rating]"
                value="3"
              />
              <label for="first-rate3">3 stars</label>

              <input
                type="radio"
                id="first-rate4"
                name="review[rating]"
                value="4"
              />
              <label for="first-rate4">4 stars</label>

              <input
                type="radio"
                id="first-rate5"
                name="review[rating]"
                value="5"
              />
              <label for="first-rate5">5 stars</label>
            </fieldset>
          </div>

          <div class="form-field">
            <label class="form-label" for="comment">Comments</label>
            <textarea
              id="comment"
              name="review[comment]"
              rows="4"
              required
            ></textarea>
          </div>

          <div class="form-actions center">
            <button class="btn-primary" type="submit">Submit</button>
          </div>
        </form>

        <hr class="show-divider" />
        <% } %>

        <!-- All reviews -->
        <!-- Display all reviews for this listing if any exist -->
        <% if (listing.reviews.length > 0) { %>
        <div class="reviews-list">
          <p class="reviews-title">All reviews</p>

          <!-- Loop through each review and display it as a card -->
          <% for (const currentReview of listing.reviews) { %>
          <article class="review-card">
            <!-- Review author information -->
            <div class="review-header">
              <span class="review-author">@<%= currentReview.author.username %></span>
            </div>
            <!-- Review comment text -->
            <p class="review-text"><%= currentReview.comment %></p>
            <!-- Star rating display (uses CSS to show stars based on rating value) -->
            <p
              class="starability-result"
              data-rating="<%= currentReview.rating %>"
            ></p>

            <!-- Show delete button only if the current user is the author of this review -->
            <% if (currUser && currUser._id.equals(currentReview.author._id)) { %>
            <form
              method="POST"
              action="/listings/<%= listing._id %>/reviews/<%= currentReview._id %>?_method=DELETE"
              class="review-delete-form"
            >
              <button class="btn-link-danger" type="submit">Delete</button>
            </form>
            <% } %>
          </article>
          <% } %>
        </div>
        <% } %>
      </section>

      <!-- Map -->
      <!-- 
        This section displays an interactive map showing the listing's location.
        The map is initialized by the external map.js script which reads data
        from the data attributes on the map div element.
      -->
      <section class="show-map-section">
        <h3>Location on map</h3>
        <!-- 
          Map container with data attributes containing listing information.
          The map.js script reads these attributes to initialize the map.
        -->
        <div 
          id="map"
          data-location="<%= listing.location %>"
          data-country="<%= listing.country %>"
          data-title="<%= listing.title %>"
        ></div>

        <div class="map-actions">
          <a
            href="https://www.google.com/maps/search/?api=1&query=<%= encodeURIComponent(listing.title + ', ' + listing.location + ', ' + listing.country) %>"
            target="_blank"
            class="btn-primary btn-inline"
          >
            Open in Maps
          </a>
        </div>
      </section>
    </div>
  </article>
</section>
